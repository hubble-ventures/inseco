{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cdn.jsdelivr.net/npm/@hubble-ventures/infisicml@2/schema/secrets.schema.json",
  "title": "Infisicml secrets manifest",
  "description": "Declares an array of Infisical vault folders and, per folder, exactly which keys to pull into a gitignored .env.secrets file next to this manifest.",
  "type": "object",
  "additionalProperties": false,
  "required": ["secrets"],
  "$defs": {
    "envVarName": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
    },
    "folderEntry": {
      "description": "A key name (string), an alias { SOURCE: \"TARGET\" }, or a subfolder { name: [ ... ] }. In an object entry, a string value is an alias target and an array value is a subfolder.",
      "oneOf": [
        { "$ref": "#/$defs/envVarName" },
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "oneOf": [
              { "$ref": "#/$defs/envVarName" },
              { "$ref": "#/$defs/folderArray" }
            ]
          }
        }
      ]
    },
    "folderArray": {
      "type": "array",
      "minItems": 1,
      "description": "A folder's contents: keys, aliases, and subfolders.",
      "items": { "$ref": "#/$defs/folderEntry" }
    },
    "folderObject": {
      "type": "object",
      "minProperties": 1,
      "description": "A folder: { folderName: [ ...contents ] }.",
      "propertyNames": { "type": "string", "pattern": "^[a-z0-9_/-]+$" },
      "additionalProperties": { "$ref": "#/$defs/folderArray" }
    },
    "secrets": {
      "type": "array",
      "minItems": 1,
      "description": "Top-level array of folder objects.",
      "items": { "$ref": "#/$defs/folderObject" }
    }
  },
  "properties": {
    "$schema": { "type": "string" },
    "secrets": { "$ref": "#/$defs/secrets" },
    "profiles": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["secrets"],
        "properties": {
          "secrets": { "$ref": "#/$defs/secrets" },
          "fetch": {
            "enum": ["folder", "keys"],
            "description": "Read strategy for this profile; replaces the root fetch when set"
          }
        }
      },
      "description": "Named alternate trees; a profile's secrets replace the root secrets when --profile is set"
    },
    "ci": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "skipWhenEnv": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Skip Infisical in CI when all listed env vars are already set"
        },
        "stubInCi": {
          "type": "boolean",
          "description": "When true, write a stub .env.secrets in CI without calling Infisical"
        }
      }
    },
    "output": {
      "type": "string",
      "default": ".env.secrets",
      "pattern": "^[^/\\\\]+$",
      "description": "Output filename only (no path separators), relative to this manifest directory"
    },
    "fetch": {
      "enum": ["folder", "keys"],
      "default": "folder",
      "description": "Read strategy. 'folder' (default) pulls whole folders and selects the declared keys locally. 'keys' fetches only the declared keys per folder; in CI the vault transmits nothing else (per-key REST reads). A per-profile 'fetch' replaces this one."
    },
    "environments": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "optionalKeys": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Declared keys allowed to be absent in this environment; a notice is logged instead of failing"
          }
        }
      },
      "description": "Per-environment policy keyed by Infisical env slug (development, preview, production)"
    }
  }
}
